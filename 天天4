#include<stdio.h>
#include<math.h>
float Area(float a, float b, float c)
{
    float S=(a+b+c)/2;
    return sqrt(S*(S-a)*(S-b)*(S-c));
}
int main()
{
    float a, b, c;
    scanf("%f%f%f", &a, &b, &c);
    printf("area=%.2f\n", Area(a, b, c));
    return 0;
}
